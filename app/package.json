{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "electron .",
    "package-mac": "electron-packager . supergame --overwrite --platform=darwin --arch=x64 --prune=true --out=../executables",
    "package-win": "electron-packager . supergame --overwrite --asar=true --platform=win32 --arch=x64 --prune=true --out=../executables",
    "package-linux": "electron-packager . supergame --overwrite --asar=true --platform=linux --arch=x64 --prune=true --out=../executables"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "electron": "^11.2.3",
    "electron-builder": "^22.9.1",
    "electron-packager": "^15.2.0"
  },
  "dependencies": {
    "bootstrap": "^4.6.0",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1",
    "standard": "^16.0.3"
  },
  "description": ""
}
